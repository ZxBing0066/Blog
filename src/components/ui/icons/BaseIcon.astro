---
// Base icon component with shared animation styles
---

<style is:global>
    /* SVG动画设置 - 优化版本 */
    /* 基础SVG元素动画设置 */
    .svg-element {
        transition: stroke-dashoffset 1.5s cubic-bezier(0.4, 0, 0.2, 1);
        stroke-dashoffset: var(--offset, 100%);
    }

    /* 填充元素的特殊动画设置 */
    .svg-fill-element {
        transition: opacity 0.5s ease-in-out;
        transition-delay: 1s;
    }

    /* 特殊SVG元素动画设置 */
    .svg-special-element {
        transition: stroke-dashoffset 1s ease-in-out;
        transition-delay: 0.8s;
    }

    /* 闪烁元素的特殊动画设置 */
    .svg-blink-element {
        animation: blink 1.2s infinite ease-in-out;
        animation-delay: 1.5s;
    }

    /* 当悬停时触发SVG元素动画 */
    .module-container:hover .svg-element {
        stroke-dashoffset: 0;
    }

    /* 当悬停时触发填充元素动画 */
    .module-container:hover .svg-fill-element {
        opacity: 1;
    }

    /* 为SVG元素添加连续动画效果 */
    .svg-container svg .svg-element:nth-child(2) {
        transition-delay: 0.2s;
    }

    .svg-container svg .svg-element:nth-child(3) {
        transition-delay: 0.4s;
    }

    .svg-container svg .svg-element:nth-child(4) {
        transition-delay: 0.6s;
    }

    .svg-container svg .svg-element:nth-child(5) {
        transition-delay: 0.8s;
    }

    /* 光标闪烁动画 */
    @keyframes blink {
        0%,
        100% {
            opacity: 0;
        }
        50% {
            opacity: 1;
        }
    }
</style>

<slot />
